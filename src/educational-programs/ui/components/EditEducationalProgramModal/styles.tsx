import { styled } from '@mui/material';
import { ModalTitle, ModalBody } from '@/common/ui/components';

export const Content = styled(ModalBody)({
  maxWidth: '460px',
});

export const Title = styled(ModalTitle)({
  marginBottom: '16px',
});
